{
  "name": "clue",
  "displayName": "clue",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.77.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Clue",
      "properties": {
        "clue.includeFiles": {
          "type": "string",
          "default": "**/*.ts",
          "definitions": "Glob pattern of files to include in the analysis"
        },
        "clue.excludeFiles": {
          "type": "string",
          "default": "**/node_modules/**",
          "definitions": "Glob pattern of files to exclude from the analysis"
        },
        "clue.analyzeBatchSize": {
          "type": "number",
          "default": 25,
          "definitions": "Number of files to analyze in a batch"
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "clue",
          "title": "Clue",
          "icon": "media/clue-activitybar.svg"
        }
      ]
    },
    "views": {
      "clue": [
        {
          "id": "statistics",
          "name": "Statistics"
        },
        {
          "id": "recognized",
          "name": "Recognized"
        },
        {
          "id": "unknown",
          "name": "Unknown"
        }
      ]
    },
    "menus": {
      "view/item/context": [
        {
          "command": "clue.item.addNote",
          "when": "view == recognized || view == unknown",
          "group": "inline"
        },
        {
          "command": "clue.item.remove",
          "when": "view == recognized && viewItem == customEntity"
        }
      ],
      "view/title": [
        {
          "command": "clue.newEntity",
          "when": "view == recognized",
          "group": "navigation"
        }
      ]
    },
    "commands": [
      {
        "command": "clue.item.show",
        "title": "Show Item"
      },
      {
        "command": "clue.item.addNote",
        "title": "Add Note",
        "icon": "$(note)"
      },
      {
        "command": "clue.item.remove",
        "title": "Remove",
        "icon": "$(remove)"
      },
      {
        "command": "clue.reanalyze",
        "title": "Re-analyze",
        "category": "Clue",
        "icon": "$(sync)"
      },
      {
        "command": "clue.newEntity",
        "title": "New Entity",
        "category": "Clue",
        "icon": "$(add)"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/eslint": "^8.37.0",
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@types/vscode": "^1.77.0",
    "@typescript-eslint/eslint-plugin": "^5.56.0",
    "@typescript-eslint/parser": "^5.56.0",
    "@vscode/test-electron": "^2.3.0",
    "eslint": "^8.36.0",
    "eslint-plugin-typeorm-analyzer": "file:../eslint-plugin-typeorm-analyzer",
    "glob": "^8.1.0",
    "mocha": "^10.2.0",
    "typescript": "^4.9.5"
  }
}
